rootProject.name = 'script-daily'

def projects = [
        'common',
        'send-mail',
        'mongo-migrate',
        'es-migrate',
        'fs-migrate',
        'json-op',
]

projects.forEach {
    include "java:$it"
}
