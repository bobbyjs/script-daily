rootProject.name = 'script-daily'

def projects = [
        'common',
        'text-find',
        'send-mail',
        'mongo-migrate',
        'es-migrate',
        'fs-migrate',
        'json-op',
        'jdbc-op',
        'eval-op'
]

projects.forEach {
    include "java:$it"
}
